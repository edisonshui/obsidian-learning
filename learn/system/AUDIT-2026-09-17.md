# Learning-system audit — 2026-09-17

Read in full: `CLAUDE.md`, `learn/system/*.md`, templates, hooks, skills, `settings.json`, `learn/me/preferences.md`. Evidence: `learn/subjects/oop/` (s01–s03, `record.md`, `resume.md`), hook state under `.claude/obsidian-live/`, mirrors under `Claude outputs/Conversations/`. Nothing edited except this file. Times are CDT.

Standard for §2: **gated** means a violation surfaces in a record (session note, `record.md`, `plan.md`, frontmatter, Dashboard) or a required verification report. A violation merely reconstructible from `log.md` is **prose** — the mirror captures everything, so counting it would gate every rule. `diagrams.md` is formatting guidance, outside scope.

## 1. Anomalies

### 1.1 `active_minutes` / `idle_minutes` are `unknown` in s02 and s03

The instrumentation measured correctly; no pipe from it to the note exists.

1. `settings.json:47–60` and `:93–107` run `obsidian-live.py` on `UserPromptSubmit` and on `PostToolUse` for `AskUserQuestion`; `obsidian-live.py:421` and `:450` call `touch_clock`.
2. `touch_clock` (`:174–207`) accumulates `clock.active` / `clock.idle` in the per-conversation state file (`:183`; keyed by `session_id`, `:393`; written `:460`). Proof it ran: `.claude/obsidian-live/df6e9fa8-a233-4002-895f-2950d2acd030.json` holds `active: 2511 s` (42 min), `idle: 6551 s` (109 min) — exactly s02 (22:01–22:35) + s03 (00:25–00:32) + the 22:35→00:24 gap.
3. **Break 1 — `:207`.** Text is returned only on a >15-minute gap (`:187`, `:195–200`) or at the 45-minute threshold (`:202–206`); an ordinary prompt gets `""`. `workflow.md:47` says the hook "reports the gap … and the running active total"; it does so only on those two events. s02 triggered neither (34 min, no gap): no clock line ever reached the model.
4. **Break 2 — `:183` / `:393`.** The accumulator is per conversation, never reset at `/learn-start`, `/learn-resume` or `/learn-end` (`subject_segments`, `:238–249`, routes text only). s02 and s03 share conversation `df6e9fa8`, so s03's one notice — 00:24, away branch — said "Active time so far: 34 minutes", s02's figure.
5. **Break 3 — `:450` → `:466` → `:485`.** Answer-path notices print from a `PostToolUse` hook; Claude Code adds plain hook stdout to context only for `UserPromptSubmit` and `SessionStart` (hooks reference, "Exit code 0"), so they are dropped — and `:203` still increments `announced`, so a 45-minute mark crossed on a picker answer is consumed undelivered and not repeated for 20 minutes.
6. Nothing reads the state back: `main` (`:471`) offers only `hook` / `status` / `backfill`; `status_text` (`:373–380`) omits the clock; `learn-status.py:266` reads the note, downstream.
7. `learn-end/SKILL.md:14` makes the writer's only source "the totals the `UserPromptSubmit` hook has been reporting", else `unknown`. Given 3–6, `unknown` was correct both times: the gate worked; the instrumentation never reached it.

s01 predates the clock: its conversation's state (`c740a9ef-….json`) has no `clock` key; `obsidian-live.py`'s mtime (20:48, 09-15) postdates s01's last turn (18:20).

### 1.2 s01 has no `active_minutes` / `idle_minutes` fields at all

Template and writers agree today: `templates/session-note.md:8–9` carries the fields, `learn-start/SKILL.md:47` and `learn-resume/SKILL.md:27` copy the template "in full", `learn-end/SKILL.md:14` fills them. The disagreement is temporal and documentary:

- s01 was created 17:19 on 09-15 from the template then in force and finalized 18:18 (mtime 18:18:51). `templates/session-note.md`, `tutor.md`, `workflow.md` (20:43), `learn-end`, `learn-check`, `settings.json` (20:44), `obsidian-live.py` (20:48) and `CLAUDE.md` (20:53) were all modified between s01's end and s02's start (22:01). s01 is pre-migration; s02 is the first note from the current template.
- Nothing back-fills: `learn-end` touches only the current note; `learn-status.py:129–136` reads whatever frontmatter exists and prints "—" when a value is missing (`:266`; `progress.md:111`).
- `records.md:65–77`, the reference every skill must read before writing a record (`CLAUDE.md:27`, `records.md:3`), lists `end`, `paused`, `nodes` — not `active_minutes` / `idle_minutes`. The schema disagrees with both (C2, §5).

### 1.3 s03 open since 00:32 on 09-16

`records.md:73–74` defines `paused` as "set by `/learn-end break`; cleared on resume"; `:79` defines cut-off as "`end` empty and no `paused`". Nothing bounds how long `paused` may stand. `learn-resume/SKILL.md:27` applies an implicit bound — "a `paused:` time from today" reopens the note — but its final branch ("Otherwise create …") opens a new note without finalizing the stale one: a pause older than its `date` is neither reopened nor closed. `workflow.md:48` promises reopening unbounded. `record.md:8` (`next:`) still says "resume paused session s03", which `learn-resume:27` will not do after 09-16. `session-start.sh:23–41` and `learn-status.py:129–136`, `:261–267` read session frontmatter and never flag an empty `end`.

The missing rule: *a `paused` note older than N hours is a cut-off; `/learn-resume` finalizes it — sets `end`, records that it was never reopened — before opening the next note; any note with `end` empty is reported at session start.* It belongs in `records.md:79`, and the bound must be elapsed time, not calendar day: `paused` is `HH:MM` and inherits the note's `date`, so a 23:50 pause resumed at 00:10 already fails the same-day test.

### 1.4 Retrieval-check gate across s01–s03

| Note | *Retrieval checks* contains | New nodes opened | Gate |
| --- | --- | --- | --- |
| s01 (`:67–68`) | nothing — heading, then blank | n1 → n2 → n3 → n4, four consecutive, no `/learn-check` (`Conversations/c740a9ef…md` has no mixed check between 17:19 and 18:18) | **Violated, silently.** No "none run — gate violated" line. |
| s02 (`:99–103`) | decay checks n3, n4; mixed check n1 + n7 labelled "gate, before n8" | n5 → n6 → n7 → check → n8 | **Enforced.** `Conversations/df6e9fa8…md:419` ("Let me log this and then move to n8") confirms the order. |
| s03 (`:39–42`) | decay checks n8, n7 | n9 only, partial | **Not reached** — fewer than four new nodes. Section non-empty only because of the decay checks. |

Two caveats. Every file carrying the gate text was modified after s01 ended (§1.2), so the vault cannot show whether the rule existed then; it shows the artifact violates the gate as written and no later `/learn-end` back-filled it. Second, the "never left empty" test (`templates/session-note.md:37`, `learn-end:14`) is satisfied on every resumed session by the decay checks `learn-resume:31` files under the same heading — the blank-section trace can fire only in a first session.

## 2. Rule inventory

| # | Rule | Source | Gated / prose | Proposed action |
| --- | --- | --- | --- | --- |
| C1 | Read the three imports before doing anything else | CLAUDE.md:3 | prose | none — untraceable |
| C2 | At start: list subjects, ask which command, no teaching until chosen | CLAUDE.md:11–15 | prose | none (= W1) |
| C3 | Learning state lives only in `learn/subjects/`; transcript and auto memory are not state | CLAUDE.md:19 | prose | none; OBSERVED drift: s02:116 sent a check-construction fix "to memory, not record.md" — G4 gives such fixes a home |
| C4 | Write as you go: each node in the note before the next starts | CLAUDE.md:20 | prose | rejected (X6) |
| C5 | `learn/system/` read-only during a lesson; propose in chat | CLAUDE.md:21 | gated — permission prompt, settings.json:15 | none |
| C6 | `preferences.md`: append to *Observed* only, with evidence; never edit *Stated* | CLAUDE.md:22 | gated — permission prompt settings.json:16; line format preferences.md:39 | none |
| C7 | Reliability rules non-negotiable; verify before the learner builds on a claim | CLAUDE.md:23 | prose (pointer to T29–T34) | rejected (X10) |
| C8 | Retrieval check is a gate: no fourth consecutive new node without a logged mixed check; skip reason under *Retrieval checks*; never empty | CLAUDE.md:24 | gated — required section, templates/session-note.md:35–37; learn-end:14 | G2(b): the section is non-empty on every resumed session (decay checks), so strengthen the test |
| C9 | Every node logs a `Diagram:` line | CLAUDE.md:25 | gated — required line, templates/session-note.md:31; learn-end:14; nothing verifies it | G2(a); OBSERVED: s01 has none for n1–n4 |
| C10 | Never claim a write you did not make; `/learn-end` re-reads and reports | CLAUDE.md:26 | gated — verification step, learn-end:46–60 | none; OBSERVED pre-rule: Conversations/c740a9ef…md:411 narrates a `preferences.md` update that never landed (mtime 13:45, *Observed* empty), no checklist printed |
| C11 | Skills describe file mechanics; read `records.md` before writing any record | CLAUDE.md:27 | prose | none |
| C12 | Generated files never hand-edited; `learn-status.py` reports contradictions, never repairs | CLAUDE.md:50 | gated — regeneration; learn-status.py:141–169 | none; see C5 in §5 |
| C13 | Mermaid/LaTeX per `diagrams.md`; wikilinks with full paths | CLAUDE.md:52 | prose | none — Obsidian shows a broken link itself |
| C14 | Tell the learner to open `log.md`; `/learn-start` and `/learn-resume` select the log | CLAUDE.md:56 | prose | none |
| C15 | Hooks write the live note; never hand-edit or re-copy the transcript; records kept separately | CLAUDE.md:58 | gated — `log.md` regenerated from state, obsidian-live.py:327–356 | none |
| C16 | A probe must stand alone: code inside the question text, no "this"/"the following" | CLAUDE.md:59 | prose | G6 (hypothetical) |
| C17 | If the learner cannot see something: resend, confirm, re-ask; do not rephrase | CLAUDE.md:60 | prose | none |
| C18 | Do not guess the active model or effort | CLAUDE.md:61 | prose | none — log.md header carries the actual model |
| T1 | Teach at the edge, from the record; never re-teach what is demonstrated | tutor.md:7 | prose (`skipped` status covers diagnosis only) | none |
| T2 | Aggregate framings; show both when both help, say which is standard | tutor.md:8 | prose | none — degree |
| T3 | Label every uncertainty | tutor.md:9 | prose | rejected (X10) |
| T4 | Struggle goes into the material; logistics are the tutor's job | tutor.md:10 | prose | none — philosophy |
| T5 | Unconditional truths first | tutor.md:14 | gated at plan level (plan "Rests on" column, templates/plan.md:29); prose in lesson | none |
| T6 | Every step motivated ("how could I have discovered this?") | tutor.md:15 | gated at plan level ("Discovery question" column); prose in lesson | none |
| T7 | One reasoning step per message | tutor.md:19 | prose | rejected (X2) |
| T8 | Check question after every non-trivial step; wait | tutor.md:20 | prose | rejected (X2) |
| T9 | Banned phrases: obviously / clearly / it's easy to see / simply | tutor.md:21 | prose | rejected (X3) |
| T10 | Don't re-teach a demonstrated prerequisite; probe an uncertain one | tutor.md:22 | prose | none (= T1) |
| T11 | Adapt on evidence: two right → bigger step; one wrong → smaller step, new representation | tutor.md:23 | prose | rejected (X8) |
| T12 | "I get it" is a claim; ask a check anyway | tutor.md:24 | prose; consequence gated by R11 | none |
| T13 | Node order motivate → establish → connect → check | tutor.md:28–32 | gated — per-node labels, templates/session-note.md:25–33 | G2(a) verifies the labels |
| T14 | Analogy only when it clarifies; always state where it breaks | tutor.md:33 | prose | rejected (X7) |
| T15 | Draw when structure/flow/decision is the point | tutor.md:34 | prose; the decision is traced by T16 | none |
| T16 | Every node logs a `Diagram:` line | tutor.md:35 | gated (= C9) | G2(a) |
| T17 | Example first by default; reverse when the record says so | tutor.md:36 | prose | none — degree |
| T18 | Hint ladder: one rung per turn, never skip; rung 4 last resort | tutor.md:40–47 | prose | rejected (X4) |
| T19 | Grade plainly; praise specific or absent; no "Great question!" | tutor.md:51 | prose | rejected (X3) |
| T20 | Wrong answer: correct it, explain the temptation, log a misconception candidate | tutor.md:52 | gated — *Misconception candidates* section, templates/session-note.md:39–41 | none — honoured in s01:71, s02:107–108, s03:46 |
| T21 | Only check/practice answers are evidence | tutor.md:53 | gated (= R10) | none |
| T22 | Diagnostic probes binary-search; record floor and ceiling with the actual Q&A | tutor.md:57 | gated — Strands table + evidence log, templates/record.md:29–31, 47 | none |
| T23 | Retrieval checks every 2–3 nodes, mixed with an older node | tutor.md:58 | gated (= C8) | none |
| T24 | MC construction: correct claim first, mutate distractors, parallel options, longest never right | tutor.md:59 | prose | G4 (OBSERVED position bias, s02:97, s02:116) |
| T25 | Free-response forms; decide rubric before reading the answer | tutor.md:60 | prose | rejected (X9) |
| T26 | Use AskUserQuestion for MC when available | tutor.md:61 | prose | none |
| T27 | Self-containment; a failed "can't see it" attempt is not evidence | tutor.md:62 | prose (= C16) | G6 |
| T28 | Retrieval checks are a gate; write the skip reason | tutor.md:63 | gated (= C8) | G2(b) |
| T29 | Never present an unsure fact as fact; label and verify | tutor.md:67 | prose | rejected (X10) |
| T30 | Numbers, versions, signatures, dates verified before plan or lesson | tutor.md:68 | gated at plan level (*Review notes*, templates/plan.md:41–43); prose in lesson | rejected (X10) |
| T31 | When wrong: say so, correct, write under *Corrections*, update the record | tutor.md:69 | gated — *Corrections* section, templates/session-note.md:43–45 | none (sentinel rejected, X5) |
| T32 | Verify answer keys against verified material | tutor.md:70 | prose | rejected (X10) |
| T33 | Prefer primary sources; when they disagree, say which is standard | tutor.md:71 | prose | rejected (X10) |
| T34 | Unresolved load-bearing check: stop and say so | tutor.md:72 | prose | rejected (X10) |
| T35 | Voice: plain, direct, warm, short; no hype, filler, emoji | tutor.md:76 | prose | rejected (X1) |
| W1 | Boot: offer resume/start; do not teach until chosen | workflow.md:20 | prose (= C2) | none |
| W2 | Goal capture: ≤3 questions together; 3–6 success criteria into `record.md` | workflow.md:24 | gated — Success criteria section, templates/record.md:17–23 (question count prose) | none |
| W3 | Diagnose 3–6 strands by binary search; 10–15 min budget; every probe → evidence; reuse other records | workflow.md:25 | gated — Strands table + evidence log (budget prose) | none |
| W4 | Plan nodes with truth, discovery question, check type, minutes, prereqs; `skipped`; mermaid DAG; 45–60-min groups | workflow.md:26 | gated — plan table, templates/plan.md:29–31; learn-status.py:161–168 checks edges vs prereqs | none |
| W5 | Review with fact-checker and plan-reviewer; unresolved uncertainty into the plan | workflow.md:27 | gated — *Review notes*, templates/plan.md:41–43 (present in oop plan.md:105–117) | none |
| W6 | Present graph + five-line summary; wait for approval | workflow.md:28 | gated — `approved:` frontmatter, templates/plan.md:4 | none |
| W7 | Open a session note, enter the teach loop | workflow.md:29 | gated — file exists | none |
| W8 | Motivate in one message; establish one step per message with checks; connect | workflow.md:35–37 | prose | rejected (X2) |
| W9 | Check, grade, log question / answer / verdict | workflow.md:38 | gated — `Check.` line, templates/session-note.md:33 | G2(a) |
| W10 | Log the node now; status planned → introduced → checked in `record.md` | workflow.md:39 | timing prose; status gated by record node table | rejected (X6) for timing |
| W11 | Gate: fourth new node waits for a logged check; empty section = broken, `/learn-end` records it | workflow.md:40 | gated (= C8) | G2(b) |
| W12 | Missing prerequisite → add a node, say so, teach it | workflow.md:42 | gated — plan node table; learn-end:28 | none |
| W13 | Run `date` when a note opens; write the start time | workflow.md:46 | gated — `start:` frontmatter | none |
| W14 | Active time drives the break; hook reports gap and running total; >15 min gap → two-line recap, not counted; trust the hook | workflow.md:47 | hook-gated for measurement (obsidian-live.py:174–207); the "reports the running total" claim is false (§1.1, C1 in §5); the recap is prose | none — §1.1 is a broken pipe, not a missing gate |
| W15 | ~45 min: say so, offer a break; `/learn-end break` first; `/learn-resume` reopens the note and skips the decay check | workflow.md:48 | partially gated — `paused:` frontmatter; hook announces at :202–206 | G3 (OBSERVED: s03 open 24 h+) |
| W16 | Transcript is never the only place; each node in the note before the next | workflow.md:49 | prose (= C4) | rejected (X6) |
| W17 | Answers in chat; the note is the notebook, not the input box | workflow.md:50 | prose | none |
| W18 | Finalize the session note: covered, checks, candidates, corrections, what worked, next step | workflow.md:56 | gated — template sections | G2(a) |
| W19 | Update `record.md`: statuses, floors/ceilings, misconceptions, what works | workflow.md:57 | gated — sections + checklist | G5 |
| W20 | Update `plan.md`: statuses, re-sequencing, next group | workflow.md:58 | gated — `updated:`; learn-status.py:150–152 status agreement | none |
| W21 | Rewrite `resume.md` from scratch, under 200 words | workflow.md:59 | gated for existence; word bound unchecked | G1 — OBSERVED: current body is 258 words |
| W22 | `preferences.md` only at the two-sighting bar; say so in chat if nothing met it; never *Stated*; contradictions under *Proposed changes* | workflow.md:60 | gated — required chat line; permission prompt | none; see C4 in §5 |
| W23 | Regenerate views with `learn-status.py` | workflow.md:61 | gated — `updated:` in generated files; session-start.sh:12 reruns it anyway | none |
| W24 | Verify, do not assume: re-read every touched file, print the checklist, report skipped/blocked | workflow.md:62 | gated — verification step | none — self-report; G1/G2/G5 add mechanical checks beside it |
| W25 | Print the resume summary in chat | workflow.md:63 | prose | none |
| W26 | Resume reads four files in full, no skimming | workflow.md:67 | prose | rejected (X12) |
| W27 | Recap of at most eight lines | workflow.md:68 | prose | none — format |
| W28 | Decay check on the last 1–2 checked nodes before new teaching; pass → `solid`, fail → `decayed` and re-establish | workflow.md:69 | gated — *Retrieval checks* + node status + evidence lines | none — honoured in s02:101–102, s03:41–42 |
| W29 | Open a new session note and continue from the plan | workflow.md:70 | gated — file exists | none |
| W30 | Changing subject: `/learn-start`; nothing in `system/` or `me/` changes; reuse shared records | workflow.md:74 | prose | none |
| R1 | Could a fresh session continue from the files alone? | records.md:5 | prose (principle) | none — learn-resume:19 detects it in the moment |
| R2 | File purposes and write modes (edited in place / appended / rewritten) | records.md:9–14 | gated — structure | none |
| R3 | Session numbers per subject, never reused; same-day sessions are s03, s04 | records.md:16 | gated — filename; duplicates unchecked | G5 |
| R4 | Node status in `record.md` and `plan.md` must agree | records.md:20 | gated — learn-status.py:150–152 | none |
| R5 | Status vocabulary and when each is set | records.md:22–29 | gated — learn-status buckets an unknown status (`:191`, `:244`); transitions unchecked | G5 |
| R6 | Strand floor/ceiling in words | records.md:31 | gated — table | none |
| R7 | Misconception status `open` / `resolved YYYY-MM-DD` with evidence | records.md:33 | gated — format | none |
| R8 | Evidence line format; only probe/check/practice/decay answers are evidence; claims are claims | records.md:37 | gated — format | none |
| R9 | Append, don't overwrite, evidence | records.md:41 | prose | rejected (X11) |
| R10 | Statuses move only on evidence: no `checked` without a logged check, no `solid` without a later-session retrieval | records.md:42 | weakly gated — *Last checked* / *Evidence* columns | G5 |
| R11 | Candidates live in the note; promote on two sightings or confirmation; resolve only after a targeted check | records.md:43 | gated — sections; criteria prose | none |
| R12 | `resume.md` is disposable, regenerated every session end | records.md:44 | gated — `updated:` | none |
| R13 | Frontmatter is the index; keep it accurate | records.md:45 | gated — Dashboard reads it; accuracy unchecked | G5 |
| R14 | Never edit `learn/system/` or *Stated* from inside a lesson | records.md:46 | gated (= C5, C6) | none |
| R15 | Preference observations only with evidence; distinguish *enjoyed* from *demonstrated* | records.md:47 | gated — line format preferences.md:39 | none |
| R16 | `record.md` frontmatter fields | records.md:51–63 | gated — session-start.sh:30 and learn-status.py read them | none |
| R17 | Session-note frontmatter: `end` empty while open; `paused` set by break, cleared on resume | records.md:65–77 | gated — frontmatter; incomplete (§1.2: omits `active_minutes`, `idle_minutes`; §1.3: no bound on `paused`) | G3; C2 |
| R18 | `end` empty and no `paused` = cut off; `/learn-resume` finalizes it first | records.md:79 | gated — frontmatter + learn-resume:27; the stale-`paused` case falls through | G3 |
| R19 | `plan.md` and `resume.md` carry `subject` and `updated` | records.md:81 | gated — frontmatter | none |

## 3. Proposed gates

Ranked OBSERVED first, then by cost ascending.

### G1 — `resume.md` word bound (OBSERVED)

- **Rule:** under 200 words — workflow.md:59, records.md:13, learn-end:32.
- **Failure prevented:** the hand-off grows until session-start.sh:36 truncates it.
- **Observed:** yes — `oop/resume.md` body is 258 words; the s02 checklist asserted "under 200 words" (Conversations/df6e9fa8…md:535): a self-report cannot carry this.
- **Mechanism:** hook check — `learn-status.py` counts body words in `consistency()` (`:141–169`), warning via *Record inconsistencies* (`:293–298`).
- **Files:** `learn-status.py` (~5 lines).
- **Afterward:** Dashboard line "oop: resume.md is 258 words (limit 200)" until fixed.

### G4 — MC answer-slot field (OBSERVED)

- **Rule:** tutor.md:59, plus a one-clause addition for Edison: vary the slot holding the correct option.
- **Failure prevented:** positional patterns the learner can exploit — noticed in the moment (s02:97), then filed to auto memory, not a record (s02:116; violates C3).
- **Observed:** yes.
- **Mechanism:** required field — every logged MC check ends `key: <slot>/<count>` (`Check.` line, templates/session-note.md:33; *Retrieval checks*, `:37`; learn-check:21; learn-end:14).
- **Files:** `templates/session-note.md`, `tutor.md:59`, `learn-check/SKILL.md`, `learn-end/SKILL.md` — text only.
- **Afterward:** a run of identical slots is visible at `/learn-end`; a missing field is the violation.

### G3 — stale `paused` closure (OBSERVED)

- **Rule:** records.md:79 extended as §1.3 states; workflow.md:48; learn-resume:27.
- **Failure prevented:** a note neither resumed nor closed (s03); a `next:` promising a resume the skill never performs.
- **Observed:** yes — s03 (`:6–7`).
- **Mechanism:** `/learn-resume` verification — the final branch of learn-resume:27 sets `end:` to the `paused` value and writes "closed at resume on YYYY-MM-DD, never reopened" under *Next step* before creating the next note; hook check — session-start.sh:23–41 prints any note with empty `end` beside the record fields it already prints.
- **Files:** `records.md:79` (the bound), `learn-resume/SKILL.md:27`, `session-start.sh` (~3 lines).
- **Afterward:** a stale note shows at every session start until closed; a closed note says so.

### G2 — session-note invariants in `learn-status.py` (OBSERVED)

- **Rule:** C8/T28/W11 (gate), C9/T16 (Diagram line), T13/W9/W18 (node entry shape).
- **Failure prevented:** a required line or section silently missing — what s01 shows and what the chat checklist, a self-report, cannot catch. Also closes the §1.4 hole.
- **Observed:** yes — s01: no `Diagram.` line in four entries (`:21–65`), *Retrieval checks* blank (`:67–68`), no sentinel.
- **Mechanism:** hook check — `learn-status.py` already reads every note (`read_sessions`, `:129–136`) and has `section()` (`:67–78`) and `consistency()`; add per note: (a) each `### nN` entry under *Lesson* has `**Diagram.**` and `**Check.**` lines; (b) with four or more entries, *Retrieval checks* contains a line beginning "Mixed check" or "none run — gate violated" (the label s02:103 already uses; templates/session-note.md:37 would name it); (c) frontmatter `nodes:` matches the headings. Warnings go to *Record inconsistencies* at every `/learn-end` (learn-end:44) and session start (session-start.sh:12).
- **Files:** `learn-status.py` (~30 lines), `templates/session-note.md:37` (name the label).
- **Afterward:** Dashboard lines like "oop s01: n1–n4 have no Diagram line" and "oop s01: 4 new nodes, no mixed check, no gate-violated line".

### G5 — `record.md` index and status consistency (HYPOTHETICAL)

- **Rule:** records.md:16, :22–29, :42, :45.
- **Failure prevented:** `sessions` ≠ note count, `last_session` ≠ newest note, a reused session number, a `checked`/`solid`/`decayed` node with an empty *Last checked* cell.
- **Observed:** no — consistent today.
- **Mechanism:** hook check in `consistency()`; reporting path as G1/G2.
- **Files:** `learn-status.py` (~15 lines).
- **Afterward:** a Dashboard line naming the field and both values.

### G6 — probe self-containment flag (HYPOTHETICAL)

- **Rule:** CLAUDE.md:59, tutor.md:62, learn-check:16.
- **Failure prevented:** a wrong answer that is evidence about the interface, logged as evidence about the learner. (s03's paused n9 check not cited.)
- **Observed:** no.
- **Mechanism:** hook check — the `PreToolUse` path that already receives every question (`obsidian-live.py:433–437`, settings.json:77–91) appends a `> [!warning]` callout under the question in `log.md` when the text has a referent ("this code", "the following", "above", "the snippet") and no fenced block. Trace only; never block.
- **Files:** `obsidian-live.py` (~10 lines).
- **Afterward:** the warning sits beside the question in `log.md`. Needs a tuned regex: "which of the following" must be excluded.

## 4. Rejected

- **X1 — Voice (T35).** Degree; the contract's own example.
- **X2 — One step per message; check after each step (T7, T8, W8).** Degree; a counter would fire on every turn.
- **X3 — Banned phrases and "Great question!" (T9, T19).** Discrete and cheap to regex in the `MessageDisplay` path, but zero hits in either conversation (`grep -i` over `Conversations/*.md`). The prose rule holds; gating tutor prose slides toward gating voice.
- **X4 — Hint ladder (T18).** A rung log fires on every node, mostly "not used"; rung order inside one file proves nothing.
- **X5 — "none" sentinel for *Corrections* / *Misconception candidates* (T31, T20).** No discrete trigger, unlike *Retrieval checks*; blank and "none" are the same self-report of absence.
- **X6 — Write-as-you-go timestamps (C4, W10, W16).** A `logged HH:MM` per node is written by the process whose honesty is in question and can be copied from callout times. A real trace needs write history, which the vault lacks (no git).
- **X7 — Analogy boundary (T14).** Requires deciding what counts as an analogy; cost exceeds the violation.
- **X8 — Adapt on evidence (T11).** Degree; the evidence log already shows step size against results.
- **X9 — Rubric before answer (T25).** Ordering inside one file is unprovable; fires on every check.
- **X10 — Lesson-time reliability rules (C7, T3, T29, T30, T32–T34).** Outcome already gated: a wrong claim lands under *Corrections* (T31), plan-time claims under *Review notes* (W5). A per-claim "verified" line would fire on nearly every message.
- **X11 — Append, don't overwrite evidence (R9).** Needs history; the checklist's "N new evidence lines" is the existing proxy.
- **X12 — Read in full (W26).** Untraceable except through recap quality.

## 5. Contradictions

Reported, not resolved.

- **C1.** workflow.md:47 (the hook "reports the gap … and the running active total") versus obsidian-live.py:195–207 (only on a >15-minute gap or the 45-minute threshold; `""` otherwise). learn-end:14 relies on the workflow.md version.
- **C2.** records.md:65–77 (session-note frontmatter without `active_minutes` / `idle_minutes`) versus templates/session-note.md:8–9, learn-end:14 and learn-status.py:266, which require them.
- **C3.** workflow.md:48 and records.md:73–74, :79 (a paused note is reopened; cut-off means no `paused`) versus learn-resume:27 (reopen only if paused "from today"; otherwise a new note, the stale one untouched) versus record.md:8 `next:` ("resume paused session s03").
- **C4.** workflow.md:60 and preferences.md:39 ("at least two checks or the learner has confirmed it") versus learn-end:36 ("at least two checks **this session**"). A pattern seen once in each of two sessions qualifies under the first, not the second; Conversations/df6e9fa8…md:517–519 shows the tutor reversing itself here.
- **C5.** CLAUDE.md:50 ("generated files are never hand-edited"; learn-status.py writes `Dashboard.md`) versus learn-start:17 ("Add a row to `learn/Dashboard.md`").
- **Tension, not contradiction.** workflow.md:69 decay-checks the *most recently* checked nodes; learn-check:13 mixes in the *longest-since-checked* one. Compatible — but each note's *Next step* (s01:81, s02:120) promised broader decay checks than workflow.md:69 allows, the rule won (s02:19, s03:19), and n2, n5, n6 stay `checked` with no re-test scheduled.
